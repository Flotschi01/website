server {
    listen 80;
    
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # This line fixes React Router refresh issues:
        try_files $uri $uri/ /index.html;
    }
}
